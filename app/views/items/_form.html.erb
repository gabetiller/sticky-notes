
<%= form_for Item.create, url: user_items_path(@user, @item), :html => {:method => :create, :class => "item-form"}  do |f| %>
  <%= f.text_area :name, size: "60x3", placeholder: "add a sticky" %>
  <%= f.submit "Create" %>
<% end %>
