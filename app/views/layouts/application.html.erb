<!DOCTYPE html>
<html>
<head>
  <title>StickyNotes</title>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%=stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css?family=Permanent+Marker' %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>



</head>

<body class="site">
  <div class="content">
    <div class="container nav-box">
      <ul class="nav navbar-nav navbar-center">
        <li class= "logo">Sticky Notes</li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="my-nav-link signed-in">Signed in as: <%= current_user.email %></li>
        <li><%= link_to 'Edit profile', edit_user_registration_path, :class => "nav-link" %></li>
        <li><%= link_to "Sign Out", destroy_user_session_path, :class => "sign-out
        nav-link", method: :delete %></li>
        <li class="my-nav-link signed-in" style="font-weight: bold; padding-top: 16px;"><%= Time.now.strftime("%l:%M:%S %p") %></li>
        <% else %>
        <li><%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link'  %></li>
        <li><%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %></li>
        <% end %>
      </ul>
      <!-- </div> -->

      <div class="container">
        <br></br>
        <% if flash[:notice] %>
        <div class="alert">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= flash[:notice] %>
        </div>
        <% elsif flash[:alert] %>
        <div class="alert">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= flash[:alert] %>
        </div>
        <% end %>

      </div>
    </div>
    <%= yield %>
  </div>
  <footer class="my-footer">
    <ul class="nav navbar-nav navbar-left">
      <li><%= link_to "Built Strong By Gabe Tiller", 'https://gabetiller.com', :class => "nav-link my-nav-link my-footer-link" %></li>
    <!-- </ul>
      <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "Sign Out", destroy_user_session_path, :class => "my-footer-link sign-out
      nav-link", method: :delete %></li>
    </ul> -->
  </footer>
</body>
</html>
